@model ProductDetailsModel
@{
    ViewData["Title"] = "Mehsul Detal";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
    <div class="single-product-area section-padding-100 clearfix">
        <div class="row">
            <div class="col-12 col-lg-7">
                <div class="single_product_thumb">

                    @*@if (Model.ProductImages.Count() == 0)
                    {
                        <div class="col-md-8">
                            <img src="~/img/@Model.Product.Image" style="width:100%;height:400px" asp-append-version="true" />
                        </div>
                    }
                    else
                    {*@
                        <div id="carouselExampleCaptions" class=" carousel slide col-md-12" data-ride="carousel">
                            <ol class="carousel-indicators" style="visibility:hidden">
                                @for (int i = 0; i < Model.ProductImages.Count(); i++)
                                {
                                    if (i == 0)
                                    {
                                        <li data-target="#carouselExampleCaptions" data-slide-to="@i" class="active"></li>
                                    }
                                    else
                                    {
                                        <li data-target="#carouselExampleCaptions" data-slide-to="@i"></li>
                                    }
                                }
                            </ol>
                            <div class="carousel-inner">
                                @{ var index = 0;}
                                @foreach (var item in Model.ProductImages)
                                {
                                    if (index == 0)
                                    {
                                        <div class="carousel-item active">
                                            <img src="~/img/@item.ImageName"  style="width:100%;height:400px" class="d-block " alt="...">
                                        </div>
                                        index++;
                                    }
                                    else
                                    {
                                        <div class="carousel-item">
                                            <img src="~/img/@item.ImageName" style="width:100%;height:400px" class="d-block ">

                                        </div>
                                    }
                                    <a class="carousel-control-prev" href="#carouselExampleCaptions" role="button" data-slide="prev">
                                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                        <span class="sr-only">Previous</span>
                                    </a>
                                    <a class="carousel-control-next" href="#carouselExampleCaptions" role="button" data-slide="next">
                                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                        <span class="sr-only">Next</span>
                                    </a>
                                }

                            </div>
                        </div>
                    
                    </div>
                </div>

            <div class="details col-md-5">

                <div class="single_product_desc">
                    <div class="product-meta-data">
                        <div class="line"></div>
                        @*<p class="product-price">$180</p>*@
                        @*<a href="product-details.html">*@
                        <h4>@Model.Product.ProductName</h4>
                        @*</a>*@

                    </div>

                    <div class="short_overview my-5">
                        <b>Qeyd:</b>
                        <p>@Model.Product.Description</p>
                    </div>                
                    <label>
                        <b>Tarix:</b>
                        <time datetime="@Model.Product.DateAdded.ToString("yyyy-MM-dd")">@Model.Product.DateAdded.ToString("dd MMMM yyyy")</time>
                    </label>


                    <div>
                        <div class="table-responsive">
                            <table>
                                <tbody>

                                    @foreach (var item in Model.ProductAttributes)
                                    {
                                        <tr>
                                            <td><b>@item.Attribute:</b></td>
                                            <td><mark>@item.Value</mark></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>

                    @*<div class="action">
            <button class="add-to-cart btn btn-danger" type="button">Add to cart</button>

        </div>*@

                    <form asp-action="AddToCart" asp-controller="Cart" method="post" class="form-inline">
                        <input type="hidden" name="ProductId" value="@Model.Product.ProductId" />
                        <div class="form-group qty">
                            <div class="input-group">
                                <span class="input-group-addon mr-2 mt-2">Say</span>
                                <input type="number" size="3" class="form-control" name="quantity" value="1" min="1" step="1">
                            </div>
                        </div>
                        <button type="submit" class="btn btn-danger"><i class="fa fa-cart-plus fa-fw"></i>Səbətə at</button>
                    </form>
                </div>

                

            </div>


    </div>


   </div>