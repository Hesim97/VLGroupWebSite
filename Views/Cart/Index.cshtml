@model Cart

@section script{
    @*<script>
        function removeItem(item, id) {
            var url='@Url.Action("RemoveItemFromCart", "Basket")';
            $.ajax({
                type: 'GET',
                url: url,
                contentType: "application/json; charset = utf-8",
                data: {id},
                successful: function (data) {
                console.log(data);
                }
            }).done(function (data) {
                $("#cartDiv").html(data);
            });
    }
    </script>*@
}
<div class="cart-table-area section-padding-100">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 col-lg-8">
                <div class="cart-title mt-50">
                    <h2>Alış-veriş kartı</h2>
                </div>

                <div class="cart-table clearfix" id="cartTable">
                    <table class="table table-responsive">
                        <thead>
                            <tr style="text-align:center">
                                <th></th>
                                <th>Ad</th>
                                @*<th>Price</th>*@
                                <th style="text-align:center">Say</th>
                            </tr>
                        </thead>
                        @{

                            @Html.Partial("Partial/_Cart", Model);
                        }
                    </table>
                </div>
            </div>
            <div class="col-12 col-lg-4">
                <a class="btn btn-danger" asp-action="Checkout" asp-controller="Cart">Sifariş Et</a>
                <a class="btn btn-dark" asp-action="List" asp-controller="Product">Alış-verişə davam</a>
                </div>
                @*<div class="col-12 col-lg-4">
                <div class="cart-summary">
                    <h5>Sizin Bilgileriniz</h5>
                    <form class="">
                        <div class="form-group">
                            <label>Ad:</label>
                            <input type="text" class="form-control" required />
                        </div>
                        <div class="form-group">
                            <label>Gmail:</label>
                            <input type="email" class="form-control" required />
                        </div>
                        <div class="form-group">
                            <label>Telefon:</label>
                            <input type="tel" class="form-control" required />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Gonder" class="btn btn-danger" required />

                        </div>
                    </form>
                    <a class="btn btn-dark" asp-action="List" asp-controller="Product">Alis-verise davam</a>
                </div>
            </div>*@
            </div>
        </div>
</div>

<script>

</script>